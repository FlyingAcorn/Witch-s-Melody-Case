namespace _GAME_.Scripts.FoodRelated.Fillings
{
    public class Lettuce : Filling
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
